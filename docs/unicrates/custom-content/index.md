# Custom Content

UniCrates allows server administrators to heavily customize their crates using JSON files. You can define custom 3D models, animation sequences, particle effects, and reward pools without writing any Java code.
